class AnimatedMenuItemInfo{
    id = "";
    url = "";
    title = "";

    constructor(id: string, url: string, title: string) {
        this.id = id;
        this.url = url;
        this.title = title;
    }
}

export default AnimatedMenuItemInfo;